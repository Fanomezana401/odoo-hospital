<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(action_appointments)d" type="action" class="oe_stat_button" icon="fa-calendar" context="{'search_default_patient_id': active_id}">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Rendez-vous</span>
                            </div>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name" placeholder="Nom complet..."/>
                            <field name="cin"/>
                        </group>
                        <group>
                            <field name="date_of_birth"/>
                            <field name="phone" widget="phone"/>
                            <field name="email" widget="email"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Dossier Médical">
                            <field name="appointment_ids" readonly="1">
                                <tree>
                                    <field name="appointment_dt"/>
                                    <field name="doctor_id"/>
                                    <field name="appointment_type"/>
                                    <field name="state" widget="badge" decoration-info="state == 'pending'" decoration-warning="state == 'ongoing'" decoration-success="state == 'done'"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Adresse">
                            <group>
                                <field name="street" string="Adresse"/>
                                <field name="city"/>
                                <field name="country_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>